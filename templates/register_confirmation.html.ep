<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1">
    <title>Atopy Note</title>
    <link rel="stylesheet" href="/css/gumby2/gumby.css">
    <link rel="stylesheet" href="/css/style.css">
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
  </head>
  <script type="text/javascript">
    $(document).ready(function() {
        $('form').submit(function() {
            var data = {};
            data.id = $('input#id').val(); 
            data.code = $('input#onetime_code').val(); 
            $.post('/confirm_registry', data, function(res) {
                if (res == 'ok'){
                    alert("ご登録ありがとうございます");
                    location.href = "/";
                }else {
                    $("#errmsg").html("認証コードが正しくありません");
                }
            });

            return false;
        });
    });
  </script>

  <body>

     <div class="navbar" id="nav1">
       <div class="row">
         <a class="toggle" gumby-trigger="#nav1 > .row > ul" href="#"><i class="icon-reorder"></i></a>
         <h1 class="four columns logo"><a href="/">ATOPY NOTE</a></h1>
         <ul class="eight columns">
           <li><a href="/register.html">Register</a></li>
           <li>
             <a href="#">Tour</a>
             <div class="dropdown">
               <ul>
                 <li><a href="#">What's about</a></li>
                 <li><a href="#">How to use</a></li>
                 <li><a href="#">Know yourself</a></li>
               </ul>
             </div>
           </li>
           <li><a href="#">Blog</a></li>
           <li><a href="#">About</a></li>
         </ul>
       </div>
     </div>

     <div class="row">
         <div class="two columns"></div>
         <div class="eight columns">
             <form class="register-form">
                <br>
                <legend><h3>登録確認</h3></legend>
                <br>
                <ul>

                    <li class="field">
                        <label class="inline" for="id">メールアドレス</label>
                        <input id="id" class="wide text input" type="text" name="id" value="<%= $id %>">
                    </li>
              
                    <li class="field">
                        <label class="inline" for="onetime_code">確認用コード</label>
                        <input id="onetime_code" class="wide input text" type="text" name="onetime_code">
                    </li>

                    <li class="field">
                        <label></label>
                        <button id="submit" class="secondary medium btn">送信する</button>
                        <span id="errmsg"></span>
                    </li>
                </ul>
              </form>
        </div>
        <div class="two columns"></div>
    </div>

</body>
</html>
